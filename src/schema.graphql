schema {
    query: Query
    mutation: Mutation
}

type Query {
    getSampleData: [SampleData]
}

type Mutation {
    
}

type SampleData {
    id: ID!
    group: String
    terminalName: String
    publishedAt: String
    count: Int
}